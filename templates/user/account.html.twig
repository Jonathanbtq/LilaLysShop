{% extends 'base.html.twig' %}

{% block title %}Account{% endblock %}

{% block body %}
<div class="prf_ctn">
    <div class="prf_wth">
        <p>Welcome {{user.email}}</p>
        <div class="prf_middle_dct">
            <div class="prf_document">
                <h2>Documents</h2>
                <ul>
                    <li><a href="{{path('bon_commande', {email: app.user.email})}}">Bons de commande</a></li>
                    <li><a href="{{path('facture', {email: app.user.email})}}">Factures</a></li>
                    <li><a href="">Service après-vente</a></li>
                </ul>
            </div>
            <div class="prf_account">
                <h2 class="h2_acc">Détails<a href="{{path('accountmodif', {email: app.user.email})}}">Modifier</a></h2>
                <p>{{user.email}}</p>
                <p>{{user.email}}</p>
                <h2 class="h2_acc">Sécurité du compte</h2>
                <a href="">Modifier les paramètres de sécurité</a>
            </div>
        </div>
    </div>
</div>
<i class="fa fa-enveloppe fa-fw" role="img" aria-label="Courriel"></i>
{% endblock %}
